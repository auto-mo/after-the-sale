# Per-visitor limit for the Demand Evidence assistant. Keyed on Cloudflare's client IP header,
# because behind the tunnel every request arrives from 127.0.0.1. The app enforces its own hourly limit too.
limit_req_zone $http_cf_connecting_ip zone=de_chat:10m rate=30r/m;
